Drop real Databricks UI screenshots here. Referenced by the capability cards:
  match.png        - 3-way match / gold_fact_invoices table
  ai_classify.png  - ai_classify SQL editor result
  uc_function.png  - get_tolerance / check_gr_policy UC function page
  decision.png     - resolution_state / decision output
  dedup.png        - duplicate detection query / vector search
  approval.png     - approval_decisions maker-checker
The UI falls back to a labeled placeholder when an image is missing.
